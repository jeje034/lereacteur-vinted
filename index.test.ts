// This passes because 1 === 1
it("Testing to see if Jest works...", () => {
    console.log(
        `-------------------------- Début des tests index pour savoir si Jest fonctionne .--------------------------------`
    );
    expect(1).toBe(1);
});
